import { FiltroCulturaPipe } from './filtro-cultura.pipe';

describe('FiltroCulturaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCulturaPipe();
    expect(pipe).toBeTruthy();
  });
});
